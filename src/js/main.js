/*const page = document.getElementById("page");

const spheres = [];

page.addEventListener("click", (event) => {
    const weight = Math.floor(Math.random() * 9) + 1;

    const sphereObject = document.createElement("div");
    sphereObject.className = "sphere";
    sphereObject.textContent = weight + "kg";
});
*/